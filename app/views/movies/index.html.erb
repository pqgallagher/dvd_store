<% if @movies_in_cart.any?  %>
  <h3>Cart</h3>
  <%= render partial: 'movie', collection: @movies_in_cart %>
  <p><%= link_to 'Remove All', remove_all_movies_path, method: :post %></p>
<% end %>

<h2>All Movies</h2>


<%= render partial: 'movie', collection: @movies %>
